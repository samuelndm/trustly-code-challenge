import styled from 'styled-components'
import { componentsDefaultStyles } from 'assets/styles'

export const Container = styled(componentsDefaultStyles.Container)`
  margin: 5rem auto;
`

export const FlexContainer = styled(componentsDefaultStyles.FlexContainer)``

export const FlexItem = styled(componentsDefaultStyles.FlexItem)``
